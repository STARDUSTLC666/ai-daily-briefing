{
  "extends": "sources.yaml",
  "profile": "public-web",
  "description": "Public official websites with agent source/copy/visual review. Social accounts are outside this edition's coverage.",
  "defaults": {
    "require_x_coverage": false
  },
  "source_filter": {
    "exclude_types": ["agent_social", "opencli_social", "rsshub"],
    "exclude_id_prefixes": ["x_"]
  }
}
